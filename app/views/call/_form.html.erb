<%= form_for [elevator, elevator.calls.new] do |f| %>
  <%= f.select :floor_request, (0...(elevator.floor + 1)).map { |i| [i, i] } %>
  <%= f.select :direction, %w( up down ) %>
  <%= f.submit "NEW CALL" %>
<% end %>
